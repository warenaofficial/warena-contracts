// const { expect } = require("chai");
// const { ethers } = require("hardhat");

// describe("Deploy WarenaToken", function () {
//   it("Should return the new greeting once it's changed", async function () {
//     const WarenaToken = await ethers.getContractFactory("WarenaToken");
//     await WarenaToken.deploy();
//   });
// });
